---
import type { ContentFull, Product } from "@/lib/types";

interface Props {
  content: ContentFull;
  products: Map<string, Product>;
}
const { content, products } = Astro.props;
const s = content.structured as {
  entrants?: string[];
  overallWinnerId?: string;
  verdict?: string;
};
const entrants = (s.entrants ?? []).map((id) => products.get(id)).filter(Boolean) as Product[];
const winner = s.overallWinnerId ? products.get(s.overallWinnerId) : null;
---

{
  entrants.length > 0 && (
    <div class="not-prose mb-10 rounded-2xl border border-line bg-surface p-6 shadow-[var(--shadow-sm)]">
      <p class="text-xs font-semibold uppercase tracking-widest text-muted">Head to head</p>
      <div class="mt-3 flex flex-wrap items-center gap-3 font-display text-xl text-ink">
        {entrants.map((p, i) => (
          <>
            {i > 0 && <span class="text-muted">vs</span>}
            <span class="font-semibold">{p.name}</span>
          </>
        ))}
      </div>
      {winner && (
        <p class="mt-4 border-t border-line pt-4">
          <span class="text-xs font-semibold uppercase tracking-wide text-accent-strong">Winner</span>
          <span class="ml-2 font-semibold text-ink">{winner.name}</span>
        </p>
      )}
      {s.verdict && <p class="mt-2 text-ink-soft">{s.verdict}</p>}
    </div>
  )
}
